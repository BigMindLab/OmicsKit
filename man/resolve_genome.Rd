% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nice_circos.R
\name{resolve_genome}
\alias{resolve_genome}
\title{Resolve cytoband and chromosome order for circos plots}
\usage{
resolve_genome(genome_build, chromosome_index = NULL)
}
\arguments{
\item{genome_build}{Character string. Genome build name, such as \code{"ce10"},
\code{"ce11"}, \code{"dm6"}, \code{"hg38"}, \code{"hg19"}, or \code{"mm10"}.}

\item{chromosome_index}{Optional character vector of chromosomes to keep and
order in the circos plot. If \code{NULL}, all available chromosomes are used.}
}
\value{
An object of class \code{"circos_genome"} with elements:
\describe{
\item{\code{genome_build}}{Requested genome build.}
\item{\code{source}}{Source of the genome information: \code{"built-in"},
\code{"UCSC"}, or \code{"UCSC-chr.len"}.}
\item{\code{cytoband}}{Data frame with columns \code{chr}, \code{start}, \code{end},
\code{name}, and \code{gieStain}.}
\item{\code{chromosomes}}{Filtered chromosome vector in plotting order.}
\item{\code{plot_type}}{Default \code{circlize} plot type vector.}
\item{\code{chr_lengths}}{Named numeric vector of chromosome lengths.}
}
}
\description{
Builds a reusable genome descriptor for \code{\link[=nice_circos]{nice_circos()}}. For \code{ce10}, \code{ce11},
and \code{dm6}, chromosome sizes are provided internally, so no UCSC download is
needed. Other genome builds are resolved with
\code{circlize::read.cytoband(species = genome_build)}.
}
\details{
This function is tolerant of different cytoband column names. It standardizes
cytoband data to \code{chr}, \code{start}, \code{end}, \code{name}, and \code{gieStain} before
plotting.
}
\section{Notes}{

For \code{ce10}, \code{ce11}, and \code{dm6}, synthetic cytobands are generated from
built-in chromosome lengths. These do not display real cytogenetic bands;
they provide chromosome axes and labels.

For UCSC-supported builds, the cytoband is obtained through
\code{circlize::read.cytoband()}. If only chromosome lengths are available,
synthetic cytobands are generated and the default plot type omits the
ideogram.
}

\examples{
ce10 <- resolve_genome("ce10")
ce10

resolve_genome("dm6", chromosome_index = c("chrX", "chr2L"))

\dontrun{
hg38 <- resolve_genome("hg38", chromosome_index = "chr17")
print(hg38)
head(hg38$cytoband)
}

}
\references{
Gu Z, Gu L, Eils R, Schlesner M, Brors B. circlize implements and enhances
circular visualization in R. Bioinformatics. 2014;30(19):2811-2812.
}
\seealso{
\code{\link[=nice_circos]{nice_circos()}}, \code{\link[=is_circos_genome]{is_circos_genome()}}
}
