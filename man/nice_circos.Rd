% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nice_circos.R
\name{nice_circos}
\alias{nice_circos}
\title{Plot genome-wide circos tracks for multi-omics data}
\usage{
nice_circos(
  genome_build = "hg38",
  data_tracks = list(),
  track_types = NULL,
  metadata = NULL,
  meta_colors = NULL,
  track_colors = NULL,
  ideogram = TRUE,
  link_data = NULL,
  export_pdf = NULL,
  custom_cytoband = NULL,
  chromosome_index = NULL,
  plot_type = NULL,
  show_legend = FALSE,
  show_labels = FALSE,
  track_height = 0.1,
  metadata_height = 0.08
)
}
\arguments{
\item{genome_build}{Character string. Genome build used to initialize the
plot, such as \code{"hg38"}, \code{"hg19"}, \code{"mm10"}, \code{"ce10"}, \code{"ce11"}, or
\code{"dm6"}.}

\item{data_tracks}{Named list of data frames, \code{GRanges}, file paths, or
track config lists. Data frames must include \code{chr}, \code{start}, \code{end}, and
optionally \code{value} and \code{name}.}

\item{track_types}{Optional character vector with one type per track.
Supported values are \code{"histogram"}, \code{"line"}, \code{"points"}, \code{"scatter"},
and \code{"mutation"}. If \code{NULL}, types are inferred from track names.}

\item{metadata}{Optional named vector, one-row data frame, or genomic data
frame with \code{chr}, \code{start}, \code{end}, and \code{group} columns for metadata rings.}

\item{meta_colors}{Optional named vector of colors for metadata groups.
Names should correspond to values in \code{metadata$group}.}

\item{track_colors}{Optional named vector or list of colors keyed by track
name.}

\item{ideogram}{Logical. If \code{TRUE}, initialize with chromosome ideogram,
axis, and labels according to \code{plot_type}.}

\item{link_data}{Optional data frame with columns \code{chr1}, \code{start1}, \code{end1},
\code{chr2}, \code{start2}, and \code{end2}.}

\item{export_pdf}{Optional PDF file path. If \code{NULL}, draws on the active
graphics device.}

\item{custom_cytoband}{Optional cytoband data frame with columns \code{chr},
\code{start}, \code{end}, \code{name}, and \code{gieStain}. Unnamed five-column UCSC-style
cytobands are also accepted.}

\item{chromosome_index}{Optional character vector of chromosomes to keep and
order.}

\item{plot_type}{Optional character vector passed as \code{plotType} to
\code{circlize::circos.initializeWithIdeogram()}. Overrides the default from
\code{\link[=resolve_genome]{resolve_genome()}}.}

\item{show_legend}{Logical. If \code{TRUE}, draw a simple legend using
\code{ComplexHeatmap}.}

\item{show_labels}{Logical. If \code{TRUE}, label mutation tracks using the
\code{name} column when present.}

\item{track_height}{Numeric scalar. Default height for data tracks.}

\item{metadata_height}{Numeric scalar. Height for metadata tracks.}
}
\value{
Invisibly returns a list with:
\describe{
\item{\code{tracks}}{Normalized data tracks used for plotting.}
\item{\code{metadata}}{Normalized metadata, or \code{NULL}.}
\item{\code{track_settings}}{Track type, color, height, y-limits, and row count.}
\item{\code{link_data}}{Validated link data, or \code{NULL}.}
\item{\code{genome_build}}{Genome build used.}
\item{\code{genome}}{\code{circos_genome} object returned by \code{\link[=resolve_genome]{resolve_genome()}},
or \code{NULL} if \code{custom_cytoband} was used.}
\item{\code{chromosomes}}{Chromosomes shown in the plot.}
\item{\code{plot_type}}{Final plot type passed to \code{circlize}.}
\item{\code{cytoband}}{Cytoband used to initialize the plot.}
\item{\code{pdf_opened}}{Logical indicating whether a PDF device was opened.}
}
}
\description{
Builds a circular genome plot with optional data tracks, metadata rings, and
genomic links using \code{circlize}.
}
\details{
Tracks must already use the selected genome build. For non-human builds
without UCSC cytobands, use \code{\link[=resolve_genome]{resolve_genome()}} support for \code{ce10}, \code{ce11},
or \code{dm6}, or provide \code{custom_cytoband}.

Input tracks can be data frames, \code{GRanges}, file paths, or configuration
lists. File paths can point to \code{.bed}, \code{.bw}, or \code{.bigwig} files. Imported
\code{score} columns are automatically mapped to \code{value}.
}
\section{Track input}{

Each data track may be:
\itemize{
\item a data frame with \code{chr}, \code{start}, \code{end}, and optionally \code{value},
\code{name};
\item a \code{GRanges} object;
\item a path to \code{.bed}, \code{.bw}, or \code{.bigwig};
\item a config list such as \code{list(path = "x.bw", type = "line", color = "steelblue", height = 0.1)}.
}
}

\section{Notes}{

Coordinates are assumed to match \code{genome_build}. This function does not
perform liftOver.

For sparse regional assays, such as methylation probes or mutation calls
restricted to a single gene window, a whole-genome circos plot may make the
signal difficult to see. In that case, use \code{chromosome_index} to focus on the
relevant chromosome.

Mutation tracks are treated as discrete points. If no \code{value} column is
present, value \code{1} is used.
}

\section{Package requirements}{

This function requires \code{circlize}. Importing \code{.bed}, \code{.bw}, or \code{.bigwig}
files requires \code{rtracklayer}. Drawing legends requires \code{ComplexHeatmap}.
}

\examples{
\dontrun{
tracks <- list(
  RNA = list(
    path = "rna_expr_hg38.bw",
    type = "histogram",
    height = 0.12
  ),
  CNV = list(
    path = "cnv_hg38.bw",
    type = "line",
    height = 0.10
  ),
  Methylation = list(
    path = "methyl_brca1_hg38.bw",
    type = "points",
    height = 0.10
  ),
  Mutation = list(
    path = "mutations_brca1_hg38.bed",
    type = "mutation",
    height = 0.08
  )
)

nice_circos(
  genome_build = "hg38",
  data_tracks = tracks,
  chromosome_index = "chr17",
  plot_type = c("axis", "labels"),
  show_labels = TRUE,
  show_legend = TRUE,
  export_pdf = "TCGA_A1_A0SH_chr17_circos.pdf"
)
}

\dontrun{
cnv <- data.frame(
  chr = c("chrI", "chrII"),
  start = c(1, 1),
  end = c(1e6, 1e6),
  value = c(0.3, -0.2)
)

nice_circos(
  genome_build = "ce10",
  data_tracks = list(CNV = cnv),
  track_types = "line"
)
}

}
\references{
Gu Z, Gu L, Eils R, Schlesner M, Brors B. circlize implements and enhances
circular visualization in R. Bioinformatics. 2014;30(19):2811-2812.
}
\seealso{
\code{\link[=resolve_genome]{resolve_genome()}}, \code{\link[=is_circos_genome]{is_circos_genome()}}
}
